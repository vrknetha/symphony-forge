/**
 * @symphony/shared — Cross-app types, constants, and validation schemas.
 *
 * Export from here:
 *   - Shared TypeScript types/interfaces
 *   - Zod schemas used on both API and web
 *   - Named constants
 *
 * Do NOT export:
 *   - NestJS-specific code
 *   - React-specific code
 *   - Environment-specific logic
 */

// Placeholder — domain-specific exports are added as modules are built
export const SYMPHONY_SHARED_VERSION = '0.1.0';
